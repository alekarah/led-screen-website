# 🧪 Тестирование проекта

Документация по автоматическому и ручному тестированию LED Screen Website.

---

## 📋 Содержание

- [Unit Tests (Автоматические тесты)](#unit-tests-автоматические-тесты)
- [CI/CD Pipeline](#cicd-pipeline)
- [Smoke Tests (Быстрая проверка)](#smoke-tests-быстрая-проверка)
- [Ручное тестирование](#ручное-тестирование)
- [Что проверяют smoke tests](#что-проверяют-smoke-tests)
- [Когда запускать тесты](#когда-запускать-тесты)
- [Troubleshooting](#troubleshooting)

---

## 🧪 Unit Tests (Автоматические тесты)

**Автоматизированные unit-тесты для проверки отдельных компонентов приложения.**

### Запуск всех тестов

```bash
cd backend
go test ./... -v
```

### Запуск с покрытием кода

```bash
cd backend
go test ./... -v -cover -coverprofile=coverage.out
go tool cover -html=coverage.out  # Открывает HTML отчет
```

### Запуск конкретных тестов

```bash
# Только handlers
go test ./internal/handlers -v

# Только middleware
go test ./internal/middleware -v

# Конкретный тест
go test ./internal/handlers -run TestGetProjects -v
```

---

### 📊 Покрытие тестами

**Текущее покрытие:**
- ✅ **Middleware (JWT Auth)** - 100% покрытие
- ✅ **Handlers (API)** - 6.5% покрытие (основные endpoints)
- ✅ **SEO (sitemap, robots)** - полное покрытие

**Тестовые файлы:**
- `backend/internal/handlers/handlers_test.go` - 9 тестов для API
- `backend/internal/handlers/seo_test.go` - 7 тестов для SEO (включая HTTPS)
- `backend/internal/middleware/auth_test.go` - 6 тестов для JWT

---

### 🔍 Что покрывают unit тесты

#### Handlers (API Endpoints)

**GetProjects:**
- ✅ Пустая база данных
- ✅ Возврат проектов из БД
- ✅ Пагинация (page, limit)

**SubmitContact:**
- ✅ Успешная отправка заявки
- ✅ Валидация обязательных полей
- ✅ Сохранение в БД со статусом "new"

**TrackProjectView:**
- ✅ Невалидный ID проекта
- ✅ Несуществующий проект (404)
- ⚠️ UPSERT просмотров (skip в SQLite)

**SEO:**
- ✅ Генерация sitemap.xml с правильным протоколом (https://)
- ✅ Правильность приоритетов страниц
- ✅ Генерация robots.txt с HTTPS sitemap
- ✅ Корректность формата robots.txt
- ✅ Поддержка X-Forwarded-Proto (nginx reverse proxy)
- ✅ HTTPS для production домена (s-n-r.ru)

#### Middleware (JWT Authorization)

**AuthMiddleware:**
- ✅ Доступ с валидным токеном (200 OK)
- ✅ Отказ без токена (редирект 302)
- ✅ Отказ с невалидным токеном (редирект 302)
- ✅ Отказ с истекшим токеном (редирект 302)
- ✅ admin_id добавляется в контекст
- ✅ Работа с множественными запросами

---

## 🚀 CI/CD Pipeline

**Автоматические тесты запускаются при каждом push через GitHub Actions.**

### Что происходит автоматически:

1. **Test Job** - Запуск всех unit тестов
   - Установка Go 1.24
   - Скачивание зависимостей
   - Запуск `go test -v -race -coverprofile=coverage.out`
   - Загрузка отчета coverage в Codecov

2. **Lint Job** - Проверка качества кода
   - Запуск golangci-lint
   - Проверка стиля кода, ошибок, безопасности

3. **Build Job** - Компиляция приложения
   - Сборка Go binary
   - Сохранение артефакта на 7 дней

### Просмотр результатов

**В GitHub:**
- Откройте Pull Request или Commit
- Вкладка "Checks" покажет результаты CI/CD
- Зеленая галочка = все прошло ✅
- Красный крестик = тесты провалились ❌

**Badges в README:**
- [![CI](https://github.com/alekarah/led-screen-website/actions/workflows/ci.yml/badge.svg)](https://github.com/alekarah/led-screen-website/actions/workflows/ci.yml)
- [![codecov](https://codecov.io/gh/alekarah/led-screen-website/branch/main/graph/badge.svg)](https://codecov.io/gh/alekarah/led-screen-website)

### Когда запускается CI/CD:

- ✅ Push в ветку `main`
- ✅ Push в ветку `develop`
- ✅ Открытие Pull Request в `main`

### Файл конфигурации:

`.github/workflows/ci.yml` - настройки GitHub Actions

---

## 🚀 Smoke Tests (Быстрая проверка)

**Автоматическая проверка критичных функций за 30 секунд.**

### Windows (PowerShell)

```powershell
# В корне проекта:
.\test-smoke.ps1
```

### Linux / Mac / Git Bash

```bash
# В корне проекта:
chmod +x test-smoke.sh
./test-smoke.sh
```

### Что происходит:

1. ✅ Проверяет зависимости (Go, PostgreSQL)
2. ✅ Собирает проект
3. ✅ Запускает тестовый сервер
4. ✅ Тестирует все критичные endpoints
5. ✅ Останавливает сервер и очищает файлы

### Пример успешного вывода:

```
========================================
  SMOKE TEST - LED Screen Website
========================================

1. Проверка зависимостей...
[PASS] Go установлен
       go version go1.24.9 windows/amd64
[PASS] PostgreSQL запущен
       PID: 12345

2. Сборка проекта...
[PASS] Проект собирается
       Бинарник создан

3. Запуск тестового сервера...
[PASS] .env файл существует
[PASS] Сервер запущен
       PID: 23456
[PASS] Сервер продолжает работать

4. Тестирование HTTP endpoints...
[PASS] Главная страница
       HTTP 200 OK
[PASS] Страница проектов
       HTTP 200 OK
[PASS] Страница услуг
       HTTP 200 OK
[PASS] Страница контактов
       HTTP 200 OK
[PASS] API /api/projects
       Получены данные проектов
[PASS] Админ логин страница
       HTTP 200 OK
[PASS] Админ панель защищена
       Требуется авторизация

5. Очистка...
[PASS] Сервер остановлен
[PASS] Временные файлы удалены

========================================
           РЕЗУЛЬТАТЫ ТЕСТОВ
========================================

Всего тестов: 14
Успешно:      14
Провалено:    0

✅ ВСЕ ТЕСТЫ ПРОШЛИ УСПЕШНО!
   Проект готов к деплою
```

---

## 🔍 Что проверяют smoke tests

### 1. Зависимости (2 теста)
- ✅ Go установлен и доступен
- ✅ PostgreSQL запущен

### 2. Сборка (1 тест)
- ✅ Проект компилируется без ошибок

### 3. Конфигурация (2 теста)
- ✅ Файл `.env` существует
- ✅ Сервер успешно запускается

### 4. HTTP Endpoints (7 тестов)

**Публичные страницы:**
- ✅ `GET /` - Главная страница
- ✅ `GET /projects` - Портфолио
- ✅ `GET /services` - Услуги
- ✅ `GET /contact` - Контакты

**API:**
- ✅ `GET /api/projects` - Список проектов (JSON)

**Админ панель:**
- ✅ `GET /admin/login` - Страница входа доступна
- ✅ `GET /admin/dashboard` - Защищена авторизацией (должен редиректить)

### 5. Очистка (2 теста)
- ✅ Тестовый сервер корректно остановлен
- ✅ Временные файлы удалены

**Всего:** 14 автоматических проверок

---

## 📝 Ручное тестирование

После успешного прохождения smoke tests рекомендуется выполнить полное ручное тестирование.

### Чек-лист ручного тестирования

#### Публичная часть (15 минут)

**Главная страница:**
- [ ] Страница загружается
- [ ] Отображаются избранные проекты (или последние 3)
- [ ] Отображаются услуги
- [ ] Работает навигация

**Портфолио:**
- [ ] Отображаются все проекты
- [ ] Фильтр по категориям работает
- [ ] Клик на проект открывает детальную страницу
- [ ] Изображения загружаются
- [ ] Пагинация работает (если >12 проектов)

**Услуги:**
- [ ] Отображаются все услуги
- [ ] Описания читаемы

**Контакты:**
- [ ] Форма обратной связи отображается
- [ ] Валидация работает (обязательные поля)
- [ ] Форма отправляется
- [ ] Показывается сообщение об успехе

**Адаптивность:**
- [ ] Сайт корректно отображается на desktop (1920px)
- [ ] Сайт корректно отображается на tablet (768px)
- [ ] Сайт корректно отображается на mobile (375px)

---

#### Админ-панель (30 минут)

**Авторизация:**
- [ ] Вход с правильными данными работает
- [ ] Вход с неправильным паролем отклоняется
- [ ] После входа редиректит на dashboard
- [ ] "Запомнить меня" работает
- [ ] Выход из системы работает

**Dashboard:**
- [ ] Отображается статистика (проекты, изображения, заявки)
- [ ] График просмотров отображается
- [ ] Счетчик заявок за 7 дней работает
- [ ] Напоминания отображаются

**Управление проектами:**
- [ ] Список проектов загружается
- [ ] Создание нового проекта работает
- [ ] Загрузка изображений работает
- [ ] Crop editor работает (перетаскивание, масштаб)
- [ ] Редактирование проекта работает
- [ ] Удаление проекта работает
- [ ] Drag & drop сортировка работает
- [ ] Переключение Featured/Active работает
- [ ] Фильтр по категориям работает

**Управление заявками:**
- [ ] Список заявок отображается
- [ ] Изменение статуса работает
- [ ] Добавление заметки работает
- [ ] Установка напоминания работает
- [ ] Фильтр по статусам работает
- [ ] Поиск по тексту работает
- [ ] Экспорт в CSV работает
- [ ] Массовые действия работают
- [ ] Удаление заявки работает

---

## ⏰ Когда запускать тесты

### Обязательно запускать:

1. **Перед каждым коммитом в Git**
   ```bash
   .\test-smoke.ps1
   git add .
   git commit -m "..."
   ```

2. **Перед деплоем на продакшен**
   ```bash
   .\test-smoke.ps1
   # Если все тесты прошли - можно деплоить
   ```

3. **После изменения кода handlers или routes**
   ```bash
   # Изменили handlers.go
   .\test-smoke.ps1
   # Убедились что ничего не сломали
   ```

4. **После обновления зависимостей**
   ```bash
   go get -u ./...
   .\test-smoke.ps1
   ```

### Рекомендуется запускать:

- После долгого перерыва в разработке
- Перед демонстрацией клиенту
- После настройки production окружения

---

## 🐛 Troubleshooting

### Тест "PostgreSQL запущен" падает

**Проблема:** PostgreSQL не запущен

**Решение:**
```bash
# Windows:
# Откройте Services → PostgreSQL → Start

# Linux:
sudo systemctl start postgresql

# Mac:
brew services start postgresql
```

---

### Тест "Проект собирается" падает

**Проблема:** Ошибка компиляции

**Решение:**
```bash
cd backend
go mod tidy
go build main.go

# Смотрите вывод ошибки и исправьте код
```

---

### Тест "Сервер запущен" падает

**Проблема:** `.env` файл отсутствует или содержит ошибки

**Решение:**
```bash
# Проверьте что .env существует:
ls backend/.env

# Проверьте содержимое:
cat backend/.env

# Убедитесь что DATABASE_URL правильный
```

**Проверьте логи:**
```bash
# PowerShell:
cat backend/test-error.log

# Bash:
cat backend/test-error.log
```

---

### Тесты HTTP endpoints падают

**Проблема:** Порт 8080 занят другим процессом

**Решение:**
```bash
# Windows (PowerShell):
Get-Process -Id (Get-NetTCPConnection -LocalPort 8080).OwningProcess

# Linux/Mac:
lsof -i :8080

# Остановите процесс или измените порт в .env
```

---

### Тест "Админ панель защищена" падает

**Проблема:** Middleware авторизации не работает

**Решение:**
1. Проверьте что JWT_SECRET установлен в `.env`
2. Проверьте что middleware подключен в `routes/routes.go`
3. Проверьте логи сервера

---

## 📊 Интерпретация результатов

### ✅ Все тесты прошли (14/14)

**Что это значит:**
- Проект собирается без ошибок
- Все критичные endpoints работают
- База данных доступна
- Авторизация настроена правильно

**Действия:**
- ✅ Можно коммитить код
- ✅ Можно деплоить на продакшен
- ✅ Можно показывать клиенту

---

### ⚠️ 1-2 теста провалились

**Что это значит:**
- Есть небольшая проблема
- Скорее всего легко исправить

**Действия:**
1. Прочитайте детали провала
2. Исправьте проблему
3. Запустите тесты снова

---

### ❌ 3+ тестов провалились

**Что это значит:**
- Серьезная проблема
- Возможно сервер не запускается

**Действия:**
1. Проверьте логи: `cat backend/test-error.log`
2. Убедитесь что PostgreSQL запущен
3. Проверьте `.env` файл
4. Запустите сервер вручную и проверьте ошибки

---

## 🚀 Следующие шаги

После успешного прохождения smoke tests:

1. **Выполните ручное тестирование** (см. чек-лист выше)
2. **Проверьте безопасность:** [docs/SECURITY_CHECKLIST.md](SECURITY_CHECKLIST.md)
3. **Подготовьте к деплою:** [docs/DEPLOYMENT.md](DEPLOYMENT.md)

---

## 💡 Возможные улучшения

### ✅ Реализовано:
- ✅ Unit тесты для handlers и middleware
- ✅ CI/CD через GitHub Actions
- ✅ Code coverage reporting (Codecov)
- ✅ Линтинг с golangci-lint

### 🔮 Планы на будущее:

**Увеличить покрытие тестами:**
- [ ] Handlers - довести до 80%+
- [ ] Database layer (CRUD операции)
- [ ] Models (валидация)

**Integration тесты:**
```go
// database_test.go
func TestProjectCRUD(t *testing.T) {
    // Тест создания/чтения/обновления/удаления с реальной БД
}
```

**E2E тесты:**
- Playwright или Cypress
- Автоматизация пользовательских сценариев
- Тестирование админ-панели
- Кросс-браузерное тестирование

**Performance тесты:**
- Load testing с k6
- Benchmark тесты в Go

**Security тесты:**
- OWASP ZAP scanning
- Dependency vulnerability scanning

---

## 📈 Статистика тестирования

**Текущее состояние:**
- ✅ 22 автоматических unit тестов
- ✅ 14 smoke tests
- ✅ CI/CD pipeline настроен
- ✅ Middleware покрытие: 100%
- ✅ SEO оптимизировано для Google/Yandex (HTTPS)
- ⚠️ Общее покрытие: ~7% (handlers + middleware)

**Цель:**
- 🎯 80%+ покрытие критичного кода
- 🎯 E2E тесты для основных сценариев
- 🎯 Automated regression testing

---

**Версия документа:** 2.0
**Дата обновления:** Ноябрь 2025

**Тестирование - это инвестиция в качество! Автоматизация тестов экономит время и предотвращает баги! ✅**
