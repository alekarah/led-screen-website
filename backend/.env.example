# ══════════════════════════════════════════════════════════════════════════════
# ⚠️  ВАЖНО ДЛЯ PRODUCTION:
# 1. Измените все пароли ниже на сильные (используйте генератор паролей)
# 2. JWT_SECRET - обязательно сгенерируйте: openssl rand -base64 32
# 3. Установите ENVIRONMENT=production
# 4. Включите SSL для базы данных (sslmode=require вместо disable)
# ══════════════════════════════════════════════════════════════════════════════

# ── App ────────────────────────────────────────────────────────────────────────
ENVIRONMENT=development        # development | production
PORT=8080
APP_VERSION=0.1.0

# ── Database (Postgres) ───────────────────────────────────────────────────────
# ⚠️  В PRODUCTION измените пароль на сильный!
# Пример: DATABASE_URL=postgres://led_user:STRONG_PASSWORD_HERE@localhost:5432/led_display_db?sslmode=require
DATABASE_URL=postgres://postgres:password123@localhost:5432/led_display_db?sslmode=disable

# Логгер и пул соединений
DB_LOG_LEVEL=warn              # silent | error | warn | info
DB_MAX_OPEN_CONNS=20
DB_MAX_IDLE_CONNS=10
DB_CONN_MAX_LIFETIME_MIN=30

# ── Email (опционально) ──────────────────────────────────────────────────────
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=
SMTP_PASSWORD=

# ── File uploads ──────────────────────────────────────────────────────────────
UPLOAD_PATH=../frontend/static/uploads
MAX_UPLOAD_SIZE=10485760       # 10 MB

# ── Authentication ─────────────────────────────────────────────────────────────
# ⚠️  КРИТИЧНО ДЛЯ БЕЗОПАСНОСТИ: Измените JWT_SECRET в production!
# Сгенерировать сильный ключ: openssl rand -base64 32
# Приложение откажется запускаться в production с дефолтным значением
JWT_SECRET=your-secret-key-change-in-production

# ── Docker Compose (опционально) ───────────────────────────────────────────────
# Переменные для docker-compose.yml
# ⚠️  В PRODUCTION установите сильные пароли!
POSTGRES_PASSWORD=password123
PGADMIN_DEFAULT_PASSWORD=admin123
